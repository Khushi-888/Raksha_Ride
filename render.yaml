services:
  - type: web
    name: raksharide
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app_enhanced:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120 --preload
    envVars:
      - key: FLASK_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true
      - key: GMAIL_EMAIL
        value: riksharide2026@gmail.com
      - key: GMAIL_APP_PASSWORD
        value: evsztunveoqilawu
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: APP_URL
        value: https://raksharide.onrender.com
      - key: ADMIN_PASSWORD
        value: RakshaAdmin@2024#Secure!
